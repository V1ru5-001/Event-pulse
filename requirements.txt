# Core
Django==6.0.5
Pillow==10.1.0

# Database
psycopg2-binary==2.9.9
dj-database-url==2.3.0

# Environment variables
python-decouple==3.8

# Static files
whitenoise==6.6.0

# Storage (for media/image uploads in production — used when USE_S3=True)
django-storages==1.14.2
boto3==1.29.7

# Payment
requests==2.31.0

# Utilities
django-extensions==3.2.3

# Development only — safe to leave installed, just not in INSTALLED_APPS
django-debug-toolbar==4.2.0